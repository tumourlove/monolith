#include "MonolithSoundPerceptionUserData.h"

// Header-defined; this TU exists so the .generated.h gets compiled into the module.
// Future virtual overrides (Draw, DrawCanvas, PostEditChangeOwner) would land here.
